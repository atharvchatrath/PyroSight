venv/
.venv/
__pycache__/
*.pt
*.onnx
.vscode/
pyrosight_*.png

# backend runtime artifacts
backend/data/
backend/models/
backend/weights/

# frontend
frontend/node_modules/
frontend/.next/
# Alternate build dir, so `next build` can run without clobbering a live dev
# server — see NEXT_DIST_DIR in frontend/next.config.mjs.
frontend/.next-build/
frontend/out/
frontend/next-env.d.ts
*.tsbuildinfo

# Custom detector training (backend/scripts/train.py)
dataset/
runs/
